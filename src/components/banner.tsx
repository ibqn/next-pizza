type Props = {}

export const Banner = (props: Props) => {
  return (
    <section className="bg-primary bg-pattern">
      <div className="container mx-auto flex min-h-[768px] items-center justify-center">
        banner
      </div>
    </section>
  )
}
